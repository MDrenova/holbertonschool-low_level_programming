#include "main.h"
/**
 * mul - function that multiplies two integers
 * main.h header file
 * @a: is parameter first num to make multiplication
 * @b: parameter second num to make multiplication
 * Return:0
 */

int mul(int a, int b)
{
	return (a * b);
}
